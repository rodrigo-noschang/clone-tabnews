# Resumo

Instalou node numa versão específica `lts/hydrogen` e deixamos isso registrado no arquivo `.nvmrc`. Dessa forma, quem chegar agora no projeto e rodar `nvm install` vai instalar a versão que queremos.